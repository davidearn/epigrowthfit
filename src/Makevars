PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)

all: $(SHLIB) build_versions

build_versions:
	"$(R_HOME)/bin$(R_ARCH_BIN)/Rscript" -e "writeLines(vapply(c(\"TMB\"), function(x) paste(x, packageVersion(x)), \"\"), con = \"../inst/build_versions\")"
